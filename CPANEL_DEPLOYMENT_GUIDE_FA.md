# 🚀 راهنمای جامع و گام‌به‌گام راه‌اندازی کافئین روی هاست معمولی cPanel

این راهنما برای اجرای بدون دردسر سایت **Caffeine Academic OS** روی انواع هاست‌های لینوکسی اشتراکی مجهز به **cPanel** (از جمله هاست‌های ایران‌سرور، پارس‌پک، هاست‌ایران، میهن‌وب‌هاست، برتینا و...) آماده شده است.

---

## 📦 بسته‌های آماده‌سازی شده برای آپلود (موجود در پوشه `cpanel_ready/`)

با اجرای دستور `npm run package:cpanel`، دو بسته آماده و مستقل ساخته شده است:

1. **`caffeine_public_html.tar.gz`** (یا محتویات پوشه `cpanel_ready/public_html/`)
   * **مخصوص**: هاست‌های اشتراکی استاندارد و معمولی (فقط PHP/Apache/LiteSpeed بدون نیاز به نودجی‌اس).
   * **مزیت**: سبک، بدون نیاز به کانفیگ پیچیده، با راه‌انداز `.htaccess` ضد خطای ۴۰۴ و بک‌اند کامل PHP API.

2. **`caffeine_nodejs_app.tar.gz`** (یا محتویات پوشه `cpanel_ready/nodejs_app/`)
   * **مخصوص**: هاست‌هایی که در سی‌پنل گزینه **Setup Node.js App** دارند.
   * **مزیت**: اجرای مستقیم سرور بک‌اند پرسرعت Express/Node.js به همراه سوکت‌های بلادرنگ و بسته‌بندی کامل.

---

## 🛠️ روش اول: راه‌اندازی روی هاست معمولی (بدون Node.js - سریع‌ترین روش)

در این روش هاست شما فقط باید یک هاست وب لینوکسی ساده با پشتیبانی از PHP 7.4 تا 8.3 باشد:

### گام ۱: آپلود فایل‌ها
1. وارد کنترل‌پنل **cPanel** شوید.
2. به بخش **File Manager** بروید و وارد پوشه `public_html` (یا ریشه ساب‌دامین خود) شوید.
3. دکمه **Upload** را در بالای صفحه بزنید و فایل `cpanel_ready/caffeine_public_html.tar.gz` (یا کل محتویات داخل پوشه `public_html`) را آپلود کنید.
4. روی فایل آپلود شده کلیک راست کرده و گزینه **Extract** را انتخاب نمایید.

### گام ۲: فعال بودن نمایش فایل‌های مخفی (Dotfiles)
مطمئن شوید فایل `.htaccess` به درستی در `public_html` قرار گرفته است:
* در گوشه بالای File Manager سی‌پنل روی دکمه **Settings** کلیک کنید.
* تیک گزینه **Show Hidden Files (dotfiles)** را فعال کرده و Save کنید تا فایل `.htaccess` نمایان باشد.

### گام ۳: بررسی فایل `.htaccess`
فایل `.htaccess` هوشمند از پیش ساخته شده، کارهای زیر را به صورت خودکار انجام می‌دهد:
* **جلوگیری از خطای ۴۰۴ هنگام رفرش**: روت‌های کلاینت مثل `/student`, `/advisor`, `/exam` به `index.html` هدایت می‌شوند.
* **فشرده‌سازی خودکار Gzip/Deflate**: کاهش حجم تبادل داده تا ۷۰٪ و افزایش سرعت لود.
* **کش هوشمند**: فونت‌ها و استایل‌ها کش شده و فایل HTML بلافاصله تازه‌سازی می‌شود.
* **هدایت درخواست‌های API**: درخواست‌های ارسالی به `/api/*` را به رابط پرسرعت `api/index.php` منتقل می‌کند.

### گام ۴: تنظیم کلید هوش مصنوعی (اختیاری)
اگر مایلید بخش هوش مصنوعی و استخراج تست از Gemini استفاده کند:
* فایل `.env` را در ریشه `public_html` بسازید یا ویرایش کنید و مقدار زیر را در آن قرار دهید:
  ```env
  GEMINI_API_KEY=کلید_شما_از_گوگل
  ```
* موتور داخلی `api/index.php` به صورت خودکار از طریق cURL به جمینای متصل می‌شود (اگر هم کلید نباشد، موتور الگوریتمی داخلی فعال است).

---

## ⚙️ روش دوم: راه‌اندازی با بخش Setup Node.js App در cPanel

اگر شرکت هاستینگ شما در سی‌پنل ابزار **Setup Node.js App** (پلاگین CloudLinux Phusion Passenger) را فعال کرده است:

### گام ۱: ساخت برنامه در cPanel
1. در داشبورد cPanel روی گزینه **Setup Node.js App** کلیک کنید.
2. دکمه **Create Application** را بزنید:
   * **Node.js version**: نسخه `20.x` یا `22.x` (یا ۱۸ به بالا) را انتخاب کنید.
   * **Application mode**: گزینه `Production` را انتخاب کنید.
   * **Application root**: نام پوشه‌ای را وارد کنید (مثلاً `caffeine-app`).
   * **Application URL**: دامنه یا ساب‌دامین مورد نظرتان را انتخاب کنید.
   * **Application startup file**: حتماً مقدار `app.js` را وارد کنید.
3. روی دکمه **Create** کلیک کنید.

### گام ۲: آپلود سورس برنامه
1. از طریق **File Manager** به همان پوشه‌ای که در Application root مشخص کردید (مثلاً `caffeine-app`) بروید.
2. فایل `cpanel_ready/caffeine_nodejs_app.tar.gz` را آپلود و **Extract** کنید.
   * این پوشه شامل فایل‌های `app.js`، `package.json`، پوشه `dist/` و `.env.example` است.

### گام ۳: نصب ماژول‌ها و راه‌اندازی
1. دوباره به صفحه **Setup Node.js App** در سی‌پنل بازگردید و اپلیکیشن را ویرایش کنید.
2. در بخش Environment Variables در صورت نیاز `GEMINI_API_KEY` و `JWT_SECRET` را اضافه نمایید.
3. دکمه **Run NPM Install** را بزنید.
4. دکمه **Restart** را بزنید تا برنامه اجرا شود.

---

## 🧭 نکات مهم و رفع عیوب احتمالی (Troubleshooting)

### ۱. اگر هنگام رفرش در صفحات زیرمجموعه خطای ۴۰۴ دریافت شد:
* اطمینان حاصل کنید که ماژول `mod_rewrite` در آپاچی هاست شما فعال است (روی تمامی هاست‌های استاندارد فعال است).
* چک کنید که فایل `.htaccess` در همان مسیری باشد که `index.html` قرار دارد.

### ۲. اگر سایت را در یک پوشه فرعی (مثلاً `domain.com/subfolder`) قرار داده‌اید:
* در فایل `.htaccess` خط `RewriteBase /` را به `RewriteBase /subfolder/` تغییر دهید.

### ۳. پخش نشدن فایل‌های صوتی پس‌زمینه (Ambient Sounds):
* در فایل `.htaccess` پیش‌فرض هدر `Accept-Ranges bytes` و تایپ `audio/mpeg` فعال است و فایل‌ها در پوشه `sounds/` به راحتی از طریق مرورگر استریم می‌شوند.

### ۴. امنیت فایل‌های دیتابیس لوکال:
* کلیه فایل‌های ذخیره‌سازی داخل پوشه `api/data/` به وسیله دستورات `Require all denied` در فایل‌های `.htaccess` داخلی محافظت شده و هیچ کاربری نمی‌تواند آن‌ها را مستقیماً از طریق مرورگر دانلود کند.
